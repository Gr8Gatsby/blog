module.exports = {
    dir: {
      input: "src", // Directory where your content files will be stored
      output: "_site", // Directory where the generated site will be output
      includes: "_includes", // Optional: folder for layouts, partials, etc.
      layouts: "_layouts" // Optional: folder for reusable layouts
    }
  };