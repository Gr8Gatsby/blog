<header class="hero">
  <div class="hero-content">
    <h1>{{ title }}</h1>
    <nav>
      <a href="{{ pathPrefix }}/">Home</a>
      <a href="{{ pathPrefix }}/pages/about/">About</a>
      <a href="{{ pathPrefix }}/posts/">Blog</a>
    </nav>
  </div>
</header>

<!-- Add this CSS to your stylesheet (e.g., styles.css) -->
<style>
  .hero {
    position: relative;
    background-image: url("{{ pathPrefix }}/images/1920_1080.jpg"); /* Set the background image path */
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    height: 500px; /* Adjust height for desktop */
    display: flex;
    justify-content: center;
    align-items: center;
    color: white;
    text-align: center;
  }

  /* Responsive background image size */
  @media (max-width: 768px) {
    .hero {
      background-image: url("{{ pathPrefix }}/images/600_1080.jpg"); /* Smaller image for mobile */
      height: 300px; /* Adjust height for mobile */
    }
  }

  .hero-content {
    position: relative;
    z-index: 2;
  }

  nav {
    margin-top: 15px;
  }

  nav a {
    color: white;
    margin: 0 20px;
    text-decoration: none;
    font-weight: bold;
  }

  nav a:hover {
    text-decoration: underline;
  }
</style>